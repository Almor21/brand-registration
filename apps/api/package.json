{
  "name": "api",
  "scripts": {
    "dev": "./venv/bin/uvicorn main:app --reload --host 0.0.0.0 --port 5000",
    "migration:create": "alembic revision --autogenerate -m",
    "migration:up": "./venv/bin/alembic upgrade head"
  }
}